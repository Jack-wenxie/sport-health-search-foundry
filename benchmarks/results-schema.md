# Results Schema

Each run should record these fields so results remain reproducible and comparable.

- `run_id`
- `as_of`
- `git_sha`
- `dataset_version`
- `workload_id`
- `metrics`
- `environment`
- `notes`
