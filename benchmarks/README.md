# Benchmarks

- `spec.md`: benchmark workload, metrics, environment, and data scale.
- `results-schema.md`: result field definitions for reproducible reporting.
- `raw/`: raw result artifacts with `as_of` dates and environment snapshots.
