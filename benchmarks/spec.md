# Benchmark Spec

- Workload / query set:
- Dataset version / hash / `as_of`:
- Environment: machine, container, CPU, RAM:
- Resource limits:
- Metrics:
  - latency p50 / p95 / p99
  - throughput
  - NDCG / Recall / Precision / MRR when qrels are available
- Acceptance thresholds:
