name: ci
on: [push, pull_request]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run verify (asserts required files + golden fixture + repo smoke)
        shell: pwsh
        run: ./scripts/verify.ps1
# CI evidence requires real assertions and a retained verification record.
